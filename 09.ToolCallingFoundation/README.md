# 09.ToolCallingFoundation
Adds Gemini tool/function calling using `Microsoft.Extensions.AI`. The package/status/policy/case services are intentionally demo implementations so the tool orchestration is easy to see.

Important: before production, put authorization, argument validation, idempotency, auditing, approval rules, timeouts, and tool-call limits around write tools.
