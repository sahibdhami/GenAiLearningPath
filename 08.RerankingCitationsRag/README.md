# 08.RerankingCitationsRag
Adds a pluggable reranking stage and deterministic citation validation. The included reranker is intentionally lightweight; replace `IReranker` with a managed ranking/cross-encoder implementation later.
